# MODULE REGISTRY

This document records all modules implemented in the platform.

Its purpose is to prevent duplicate subsystem creation during AI-assisted development.

Before implementing any build, AI agents must consult this registry.

If a module already exists, it must be reused rather than recreated.

---

# MODULE TYPES

Modules are categorized into the following groups:

- API Endpoints
- Services
- Processing Engines
- Firestore Collections
- Frontend Modules
- Contract Manifests

---

# CONTRACT MANIFESTS

Manifest | Build/Task | Purpose
---|---|---
CanonicalApiRouteManifest | BWM-002, BWM-012, BWM-013, BWM-014, BWM-015, BWM-016, BWM-017, BWM-018, BWM-019, BWM-020 | `functions/src/apiRouteManifest.ts` is the machine-readable inventory for all 36 canonical frontend method/path contracts and all 48 current HTTP Functions exports. It records canonical `/api/v1` paths, current frontend paths, portal ownership, compatibility or retirement status, mapped Functions exports, the single `apiV1` gateway export, and explicit dispositions for the 18 legacy exports without frontend routes. `functions/src/api/apiGateway.ts` resolves exact method/path pairs directly from this manifest, preserves decoded parameters without maintaining a second route table, and emits structured 404/405 failures; `functions/src/api/apiGatewayHandlers.ts` maps only the manifest's 29 non-null export names to their existing raw handlers and fails startup on registry drift. `docs/api_contract.md` is the prose contract and `docs/FRONTEND_API_CALL_INVENTORY.md` contains per-route request/response evidence, including BWM-019's strict EXM-01 runtime snapshot and BWM-020's EXM-05 lifecycle authority.
SharedCapabilityMatrix | BWM-008 | `shared/contracts/capabilityPolicy.ts` is the canonical role, minimum-license, role-specific license override, and required institute feature-flag matrix for portal, Admin, Student, Exam, and Vendor capabilities. `docs/CAPABILITY_POLICY.md` explains its fail-closed three-axis rules. Admin teacher-visible routes and Student/Vendor protected-route admission consume matrix role sets directly; the aligned deployable Admin Functions handlers consume `functions/src/policy/adminRolePolicy.ts`, whose exact role sets are contract-checked against the canonical matrix. Remaining BWM-008 substeps own tenant targeting and comprehensive negative boundary coverage.

Current canonical route totals as of 2026-08-30: 31 `implemented`, 3 `incompatible`, 0 `missing`, and 1 `intentionally_retired`.

---

# API ENDPOINTS

The table below is an implementation-history registry and retains the legacy paths used by the original builds. Canonical browser route keys and current compatibility status are authoritative in `functions/src/apiRouteManifest.ts` and `docs/api_contract.md`. Do not infer a public `/api/v1` route for an endpoint that the typed manifest marks `unmapped_portal`, `internal_only`, `webhook`, or `healthcheck`.

Endpoint | Build | Description
---|---|---
GET /student/dashboard | BWM-015 | Return the verified active Student's current-year `studentYearMetrics` projection plus future assigned runs permitted by the identity license, with higher-layer metrics redacted and no raw session/question data
GET /student/tests | BWM-015 | Return bounded status/page summaries for only the verified active Student's current-year assigned runs and identity-license modes; stopped/cancelled records form the archived summary view
POST /admin/tests | TBD | Create test template
POST /admin/runs | Build 21-23, BWM-014-A | Create a scheduled test assignment from a shared strict DTO, deriving immutable template authority and a deterministic backend run ID while enforcing current-year, expected-version, recipient, license, retry, and concurrency rules
GET /admin/runs | BWM-014-C | List only the identity tenant's current-academic-year runs through a bounded deterministic cursor, with an optional strict lifecycle-status filter and shared response DTO
GET /admin/runs/{runId} | BWM-014-C | Load one complete identity-tenant/current-year run through a shared strict DTO, returning not found for missing, archived-year, or cross-tenant IDs
POST /admin/academicYear/archive | Build 101 | Archive a single institute academic year through a double-confirmed admin/vendor request that locks the year, validates no active sessions or non-terminal runs remain, exports submitted session summaries to institute-scoped BigQuery cold storage, generates the final immutable governance snapshot, writes an institute audit log, and marks `institutes/{instituteId}/academicYears/{yearId}` as `archived` idempotently
POST /admin/students/data-export | Build 103 | Generate an admin-approved student data export bundle for a single institute student by validating admin/director/vendor authorization, collecting the student profile plus `studentYearMetrics`, session history, and optional `insightSnapshots`, uploading a temporary CSV bundle to secure report storage, issuing a signed download URL, and appending an immutable institute `DATA_EXPORT` audit log with `requestedBy`, `approvedBy`, `exportHash`, and `expiresAt`
POST /admin/students/soft-delete | Build 104 / BWM-009 | Soft-delete a single institute student by validating admin/vendor authorization, setting `institutes/{instituteId}/students/{studentId}.deleted = true` idempotently, preserving session and analytics history, excluding the student from operational query flows, clearing managed identity claims, revoking Firebase refresh tokens, and appending an immutable institute `SOFT_DELETE_STUDENT` audit log
POST /admin/interventions | Build 124 | Execute teacher/admin intervention actions for high-risk students by validating institute + L1+ access, logging `ASSIGN_REMEDIAL_TEST`, `SEND_INTERVENTION_ALERT`, and `UPDATE_INTERVENTION_OUTCOME` into immutable institute `auditLogs`, and listing intervention timelines by academic year without introducing new Firestore collections
POST /admin/settings | Build 125 / BWM-009 | Execute admin-only institute settings actions (`GET_SETTINGS_SNAPSHOT`, `UPDATE_INSTITUTE_PROFILE`, `LOCK_ACADEMIC_YEAR`, `UPDATE_EXECUTION_POLICY`, `UPSERT_USER_ACCESS`, `REMOVE_USER_ACCESS`, `RESET_USER_PASSWORD`, `UPDATE_SECURITY_SETTINGS`, `UPDATE_FEATURE_FLAGS`) through a single secured endpoint that enforces tenant + role middleware, validates typed payloads, updates existing configuration documents, synchronizes/revokes affected staff identity sessions after access and security changes, and writes immutable institute-scoped settings mutation audit entries
POST /admin/governance/reports | Build 90 | Generate institute governance reports from immutable monthly governance snapshots, institute override logs, and institute audit logs, returning monthly governance summaries, reportable incident alerts, discipline deviation outputs, and PDF export metadata for `/admin/governance/reports`
POST /admin/governance/snapshots | Build 89 | Read governance snapshot summaries for a single academic year with `director` + `L3` institute access enforcement and `vendor` cross-institute access, returning only immutable governance snapshot summary records
POST /exam/start | Build 26 / BWM-017 | Start or resume exactly one eligible session from verified Student tenant/UID/license plus run ID, derive the current operational academic year server-side, converge concurrent/retried starts through a deterministic session ID and transaction, and return a strict created/replayed/resumed absolute Exam launch contract with a distinct nonce-bearing credential
POST /exam/session/{sessionId}/entry | BWM-018, BWM-019, BWM-022 | Verify the Firebase ID token with Student and complete session-bound claims; atomically consume the matching raw launch credential exactly once on `{ token }`, or recover the same persisted owner/session on authenticated `{ resume: true }`; return the strict immutable candidate-safe runtime snapshot
POST /exam/session/{sessionId}/activate | BWM-020 | Require a refreshed session-bound Student Firebase ID token, derive every identifier from its complete exact claims, idempotently persist `started -> active` with server-owned `startedAt` and immutable scheduled `deadlineAt`, return `serverTime` for skew compensation, and reconcile reached deadlines to `expired`
POST /exam/session/{sessionId}/answers | Build 30, Build 33, Build 34, Build 35, BWM-018, BWM-021, BWM-022 | Require exact refreshed session-bound identity and active pre-deadline authority; strictly validate max-ten batch identity/sequence/reason and positive client revisions; persist runtime-question-validated responses with idempotent timing; return exact revision dispositions; permit interval bypass only for serialized reconnect/submission drains
POST /exam/session/{sessionId}/submit | Build 36, Build 37, Build 38, Build 40, BWM-018, BWM-023 | Require a refreshed session-bound Firebase ID token whose institute/year/run/session/Student claims match the route and exact four-field body; finalize active manual or server-confirmed reached-deadline expiry submissions atomically; and return one strict server-owned status/reason/time/metrics result whose parallel and repeated callers replay exactly
POST /internal/email/queue | Build 48 | Enqueue backend-only asynchronous email jobs with Bearer authentication, backend-service role enforcement, payload validation, and `payload.instituteId` tenant matching
POST /vendor/simulation/environment | Build 76 | Initialize an isolated synthetic institute namespace at `institutes/sim_{simulationId}` with vendor-only access, environment safety gating, versioned simulation metadata, and idempotent create-only semantics
POST /vendor/simulation/students | Build 77 | Generate deterministic synthetic student profiles under `institutes/sim_{simulationId}/students` with vendor-only access, simulation-environment prerequisites, default topic-strength seeding, and idempotent create-only writes
POST /vendor/simulation/sessions | Build 78 | Generate deterministic submitted session documents under `institutes/sim_{simulationId}/academicYears/{yearId}/runs/{runId}/sessions/{sessionId}` with vendor-only access, Build 76/77 prerequisites, realistic answer/timing behavior, production-shaped submission metrics, and idempotent create-only writes that avoid live analytics, billing, email, and notification triggers
POST /vendor/simulation/load | Build 79 | Execute vendor-only sandbox load simulation against `institutes/sim_{simulationId}` synthetic data, sample session-start/write/submission/analytics/dashboard-read scenarios, and persist a versioned load report under `vendor/simulationReports/reports/{reportId}` without touching production billing, email, or live institute namespaces
POST /vendor/simulation/validation | Build 80 | Execute vendor-only intelligence validation against sandbox analytics outputs, compare expected synthetic risk/pattern behavior with actual `studentYearMetrics` and `runAnalytics` outputs, and persist a versioned validation report under `vendor/simulationReports/reports/{reportId}`
POST /api/stripe/webhook | Build 95 / BWM-009 | Process Stripe webhook events with signature validation, idempotent event logging, institute resolution from Stripe references or metadata, authoritative license synchronization to `license/current` and `license/main`, immutable-history-derived license versioning, retryable institute claim synchronization/session revocation, invoice persistence in `billingRecords/{invoiceId}`, billing snapshot webhook-status synchronization, and immutable vendor audit logging
POST /vendor/license/update | Build 93, Build 94 / BWM-009 | Update the authoritative institute license through a vendor-only endpoint that validates requested layer and billing-plan inputs against vendor pricing configuration, writes one history-derived `licenseVersion` to institute/current/compatibility records, atomically appends an immutable `institutes/{instituteId}/licenseHistory/{entryId}` record, then synchronizes that version/layer into every institute staff and Student identity before revoking prior sessions
POST /vendor/intelligence/initialize | Build 81 | Initialize the vendor business intelligence subsystem through vendor-only aggregate-source readiness checks across `vendorAggregates`, `billingSnapshots`, `licenseHistory`, `governanceSnapshots`, and `usageMeter`, returning typed module status without computing future revenue or churn outputs
POST /vendor/intelligence/revenue | Build 82 | Compute vendor-only revenue intelligence from aggregated `billingSnapshots` and `vendorAggregates`, returning MRR, ARR, revenue-by-layer, institute revenue ranking, ARPI/ARPS, month-over-month growth, and revenue volatility without reading raw institute operational data
POST /vendor/intelligence/layer-distribution | Build 83 | Compute vendor-only license layer distribution analytics from aggregated `vendorAggregates` and `licenseHistory`, returning current layer percentages, migration velocity, average time in layer, and upgrade frequency by institute size without reading raw institute operational data
POST /vendor/intelligence/churn | Build 84 | Compute vendor-only churn tracking analytics from aggregated `billingSnapshots`, `vendorAggregates`, `licenseHistory`, and `usageMeter`, returning monthly churn, churn by layer and institute size, inactive institutes, current-cycle downgrades, and active-student engagement decline without reading raw institute operational data
POST /vendor/intelligence/revenue-forecasting | Build 85 | Compute vendor-only revenue forecasting analytics from aggregated `billingSnapshots`, `vendorAggregates`, `licenseHistory`, and `usageMeter`, returning projected revenue growth, institute acquisition, upgrade probability, student volume trends, and infrastructure cost-to-revenue ratios without reading raw institute operational data
POST /vendor/calibration/simulate | Build 98 | Run vendor-only calibration impact simulations across selected institutes using aggregated `studentYearMetrics` only, resolving current institute calibration baselines, projecting before/after risk distributions, and returning deterministic comparison summaries without reading raw session data

---

# SERVICES

Service | Build | Purpose
---|---|---
AdminQuestionLibraryService | BWM-012 | Reload institute-scoped question records through ADM-06, preserve authoritative answer/version metadata, accept only canonical relative PNG/WebP question and solution paths, and expose fresh 30-minute dashboard-signed CDN previews without leaking direct Storage URLs, bucket names, or object paths
QuestionBulkUploadService | Build 13 / BWM-012 | Validate question batches and select authoritative IDs/versions; on commit fingerprint the normalized tenant payload, atomically persist question documents plus deterministic immutable upload/audit records, replay exact retries without rewriting, and reject structural mutation of used questions
QuestionAssetUploadService | BWM-012 | Validate PNG/WebP asset payloads against the authoritative institute/question/version tuple, persist SHA-256 metadata at canonical versioned paths with create-only Storage preconditions, replay identical bytes safely, reject conflicting content, and create deterministic retry-recoverable institute audits while exposing only the safe public result
SessionService | Build 26, Build 27, Build 28, Build 29, Build 31, Build 32, Build 33, Build 100, Build 105, BWM-017, BWM-018, BWM-019, BWM-020, BWM-022 | Manage exam session creation and identity-derived start/resume idempotency; resolve current HOT academic-year authority, assignment, active Student, current license and permitted mode; converge concurrent starts on one deterministic institute/year/run/Student document; locate the same eligible session for replay/resume; store only bounded launch-credential hashes; validate and consume launch credentials while advancing `created -> started`; recover the already-owned session from persisted Firebase identity after consumption; freeze and validate an immutable candidate-safe runtime snapshot; securely activate and reconcile lifecycle/deadline authority; enforce forward-only transitions plus reason-aware answer batching constraints; and block non-HOT years from operational access
ExamStartApi | Build 26 / BWM-017 | HTTP API handler for `POST /exam/start` that accepts only shared start/resume intent plus run ID, derives Student authority from the verified Firebase identity, and returns the strict shared session/disposition/status plus distinct nonce-bearing credential and matching absolute Exam URL
ExamRuntimeAuthentication | BWM-018, BWM-019, BWM-020, BWM-022 | Parse nested Firebase claims, remove the launch credential from URL/history, exchange it once through per-origin Firebase Auth, resume token-free reloads from persisted Firebase Auth with `{ resume: true }`, use refreshed ID tokens for all runtime APIs, strictly adapt EXM-01/EXM-05, hydrate only server authority, and derive countdown from server clock/deadline
AnswerBatchService | Build 30, Build 33, Build 34, Build 35, BWM-020, BWM-021, BWM-022 | Persist max-ten incremental batches only for active pre-deadline sessions; validate strict responses, batch identity/sequence/reason, and positive client revisions; return an exact persisted/ignored/blocked acknowledgement per question/revision; allow minimum-interval bypass only for reconnect/submission drains; retain explicit clears, absolute idempotent timing, stale/conflict rules, enforcement, and typed timing metrics
SubmissionService | Build 36, Build 37, Build 38, Build 105, BWM-023 | Execute atomic session submission transactions, enforce session ownership and HOT-year constraints, derive manual/expiry reason from the persisted deadline, acquire and release `submissionLock`, make parallel callers wait for and replay the one stored finalization, compute deterministic scoring and behavioral metrics, persist server-owned status/reason/time plus the complete result, and return exact idempotent replays without recomputation
SubmissionAnalyticsTriggerService | Build 39, BWM-024 | Detect only real `sessions/{sessionId}` transitions to `submitted`; transactionally queue deterministic `processingMarkers/{sessionId}` under the owning run and Student summaries; publish explicit newest-session `resultPropagation` processing/available state with 2/0-second retry authority; and prevent replay or an older event from resetting newer availability
RunAnalyticsEngineService | Build 41, BWM-024 | Incrementally aggregate each submitted session exactly once through a deterministic per-session marker into `institutes/{instituteId}/academicYears/{yearId}/runAnalytics/{runId}`; update metadata, averages, completion rate, standard deviation, risk distribution, and internal score histograms without raw-session scans; and mark the run plus analytics summary completed when every recipient has submitted
QuestionAnalyticsEngineService | Build 42, BWM-024 | Incrementally update `institutes/{instituteId}/questionAnalytics/{questionId}` from submitted-session payloads plus existing question analytics state, including correct/incorrect attempt counts, average response time, guess rate, overstay rate, average raw/accuracy when used, discipline stress, and risk impact, with deterministic `processingMarkers/{sessionId}` replay protection per question
StudentMetricsEngineService | Build 43, Build 87, BWM-024 | Incrementally update each Student's yearly aggregates and governance-ready trends exactly once through deterministic `processingMarkers/{sessionId}`, and persist one summary-only completed result at `studentYearMetrics/{studentId}/results/{runId}` for Student dashboard, My Tests, and performance consumers without scanning raw sessions
RiskEngineService | Build 44 | Compute idempotent student-level behavioral risk classification from `studentYearMetrics/{studentId}` updates, persist `riskScore`, `riskState`, `disciplineIndex`, `rollingRiskScore`, `rollingRiskCluster`, and `riskModelVersion`, and maintain a rolling five-evaluation risk window in processing markers without scanning raw sessions
PatternEngineService | Build 45 | Compute rolling behavioral pattern state from `studentYearMetrics/{studentId}` updates, persist pattern flags (`rush`, `easyNeglect`, `hardBias`, `skipBurst`, `wrongStreak`), escalation recommendations, and a five-session rolling summary window in processing markers without reading raw session collections
InsightEngineService | Build 46 | Generate idempotent student-level, run-level, and batch-level insight snapshots in `institutes/{instituteId}/academicYears/{yearId}/insightSnapshots/{snapshotId}` by combining submitted-session payloads with `runAnalytics/{runId}` and `studentYearMetrics/{studentId}` aggregates
NotificationQueueGenerationService | Build 47 | Generate deterministic root-level `emailQueue/{jobId}` jobs for high-risk alerts, exceptional performance recognition, and discipline notifications by combining submitted-session payloads with `institutes/{instituteId}/students/{studentId}` and `studentYearMetrics/{studentId}` inputs
EmailQueueService | Build 48 | Persist root-level `emailQueue/{jobId}` documents for backend-triggered notifications with typed payload validation, pending status initialization, retry counters, and structured logging
ApiResponseService | Build 49 | Provide standardized API error response generation with nested `error` and `meta` contracts, deterministic status-code mapping, and shared request trace metadata for backend HTTP handlers
EndpointTestingFramework | Build 50 | Provide reusable dependency-injected HTTP handler factories, shared mock request/response utilities, and deterministic endpoint contract tests for authentication, role, tenant, license, payload, and structured error response scenarios
MiddlewareFrameworkService | Build 61 | Provide a reusable middleware pipeline with ordered execution, request-context attachment, method guards, request-validation hooks, and centralized middleware failure handling for backend HTTP handlers
AuthenticationMiddlewareService | Build 62 / BWM-008 / BWM-009 | Provide shared Firebase ID token Bearer parsing and verification; require every production Firebase verifier to check token revocation; immediately reject truthy `isSuspended` claims with canonical `403 FORBIDDEN` before request-context attachment or downstream work; validate required claims (`uid`, `role`, `licenseLayer`); attach normalized identity context including a token-derived Student ID for Student roles; and optionally retain compatible studentId request data for session endpoints
CustomClaimSynchronizationService | BWM-009 | Derive institute-bound Firebase custom claims, including `licenseVersion`, from authoritative institute staff/student identity state plus the current institute license, preserve only unmanaged external claims, fail closed on missing/conflicting/invalid authority, and avoid redundant Auth writes when the projection is unchanged
IdentitySessionSecurityService | BWM-009 | Coordinate authoritative claim synchronization or managed-claim clearing before Firebase refresh-token revocation, treat missing Auth users as already session-safe, and provide the shared boundary used by staff access/security mutations plus Student deactivation and soft deletion
LicenseClaimFreshnessService | BWM-009 | Enumerate authoritative institute staff and Student identities, project the current history-derived license layer/version in bounded batches, revoke prior refresh-token sessions, safely skip superseded mutation versions, and report missing Auth identities without treating them as active sessions
TenantGuardMiddlewareService | Build 63 / BWM-008 | Provide fail-closed institute isolation: tenant-bound identities require a non-empty verified institute claim, request institute targets must match it, `TENANT_MISMATCH` is deterministic, and Vendor bypass defaults off and must be explicitly enabled by each reviewed mixed-role handler
GovernanceAccessMiddlewareService | Build 89 | Enforce governance access as `vendor` bypass or institute-scoped `director` access with required `L3` licensing for governance snapshot reads
RoleAuthorizationMiddlewareService | Build 64 | Provide shared endpoint-level role authorization middleware that enforces declared `allowedRoles` against authenticated request context, normalizes role comparisons, and emits standardized `FORBIDDEN` responses for unauthorized roles
LicenseEnforcementMiddlewareService | Build 65 | Provide shared endpoint-level license enforcement middleware that validates authenticated `licenseLayer` claims against a declared `requiredLayer` hierarchy (`L0 < L1 < L2 < L3`) and emits standardized `LICENSE_RESTRICTED` responses for insufficient license access
LicenseService | Build 93 / BWM-009 | Vendor license management service that validates institute license updates against vendor pricing configuration, resolves plan-derived feature flags and active-student limits, persists one immutable-history-derived `licenseVersion` across institute/current/compatibility records, and synchronizes/revokes every institute identity after commit
PaymentEventIntegrationService | Build 95 / BWM-009 | Verify Stripe webhook signatures, normalize supported payment/subscription events, resolve institutes from webhook metadata or stored Stripe identifiers, update authoritative institute license state and immutable `licenseHistory`, persist the history ID as the license/claim version, retry incomplete claim freshness propagation on duplicate delivery, persist invoice records, synchronize `billingSnapshots` webhook status, and emit vendor audit logs with event deduplication at `vendor/stripeEvents/events/{eventId}`
BillingService | Phase 19 | Billing computation and Stripe sync
EnvironmentConfigLoader | Build 2 | Centralized environment variable and endpoint configuration loader
SecretManagerService | Build 3 | Resolve backend secrets from local environment variables or Google Secret Manager
StructuredLogger | Build 4 | Centralized structured Cloud Logging service with request tracing and log level enforcement
RuntimeErrorReporter | Build 5 | Centralized Google Cloud Error Reporting integration for handled and unhandled runtime failures
AuditLogStorageService | Build 6 | Persist immutable global, vendor, and institute audit records in Firestore
AdministrativeActionLoggingService | Build 7 | Generate architecture-aligned administrative audit events for institute and vendor operations
LicenseHistoryService | Build 8, Build 94 | Persist immutable institute license mutation history records and prepare validated history-entry payloads for transactional reuse inside the vendor license update workflow
CalibrationVersionStorageService | Build 96 | Persist immutable calibration model version documents, validate behavioral weights and thresholds, require activation dates for active versions, store the canonical record at `globalCalibration/{versionId}`, and mirror the same payload to `calibrationVersions/{versionId}` for Build 96 flow compatibility
CalibrationDeploymentService | Build 97, Build 99 | Deploy active global calibration versions to selected institutes by validating target institutes, writing `institutes/{instituteId}/calibration/{versionId}`, updating institute-root `calibrationVersion`, mirroring the deployed calibration version into `license/current` and `license/main`, and appending immutable calibration deployment history records for vendor rollback/auditability
CalibrationSimulationService | Build 98 | Simulate proposed calibration weights against aggregated `studentYearMetrics` for selected institutes, resolve current institute or active global calibration baselines, and return projected before/after/delta risk distributions without scanning raw sessions
CalibrationHistoryService | Build 99 | Persist immutable vendor calibration deployment logs at `vendorCalibrationLogs/{logId}` and mirror the same deployment history into `institutes/{instituteId}/calibrationHistory/{entryId}` for architecture-aligned auditability and rollback tracing
OverrideLoggingService | Build 9 | Persist immutable institute execution override records
AuditTamperProtectionRules | Build 10 | Enforce append-only Firestore protection for immutable audit, license history, and override log collections
QuestionIngestionService | Build 11, Build 108 | Validate question-bank create/update payloads, normalize tags, delegate search token indexing, initialize question analytics on first ingestion, and apply delta-based dictionary updates for newly introduced tags/chapters during search-index refresh events
SearchTokenIndexService | Build 12 | Generate deterministic lightweight search tokens from subject, chapter, tags, and question text keywords during question ingestion
QuestionSearchQueryService | Build 13, Build 51, Build 52, Build 54 | Execute indexed and paginated institute question-bank retrieval for examType+subject, subject+chapter, difficulty+subject, primaryTag, and exact-token `searchTokens` filters, with shared search-domain role enforcement, `array-contains` token matching, primaryTag field indexing, and deterministic `createdAt`/`usedCount` cursor pagination
SearchArchitectureService | Build 51 | Initialize deterministic search-domain definitions, role restrictions, pagination limits, academic-year summary collection scoping, and approved query-pattern enforcement for question-bank, student, analytics, and vendor aggregate search flows
CdnArchitectureService | Build 71 / BWM-012 | Initialize deterministic CDN asset-delivery configuration from explicit overrides or the validated `CDN_BASE_URL`, `QUESTION_ASSETS_BUCKET`, and `REPORTS_BUCKET` runtime environment, secure bucket bindings, cache policies, versioned question/report asset path resolution, and direct-bucket-URL exposure prevention
StorageBucketArchitectureService | Build 72 | Formalize deterministic question-asset and reports bucket topology, directory templates, Firebase Admin bucket-handle access, storage target metadata, and bucket/object-path validation aligned to the Section 38.3 storage architecture
SignedUrlService | Build 73 | Generate CDN-domain signed URLs for question assets, downloadable reports, and restricted media paths with architecture-defined exam/dashboard expiry windows, base64url signing-key validation, reserved-query-parameter protection, and direct-bucket-URL exposure prevention
CdnCachePolicyService | Build 74 | Resolve architecture-aligned hot, warm, and cold CDN cache tiers plus final Cache-Control headers for active academic-year, inactive 30-day, and archived academic-year assets using the existing CDN architecture policy definitions
CdnMonitoringService | Build 75 | Evaluate and summarize CDN asset-delivery health for vendor system monitoring using cache hit ratio, edge latency, bandwidth usage, and HTTP 4xx/5xx indicators aligned to Section 38.18 performance targets
SimulationEnvironmentService | Build 76 | Initialize isolated synthetic institute namespaces in `institutes/sim_{simulationId}`, enforce non-production safety rules, persist versioned simulation metadata (`simulationVersion`, `calibrationVersion`, `riskModelVersion`, `parameterSnapshot`, `runCount`, `studentCount`), and return idempotent environment creation results for vendor-triggered simulation workflows
SimulationSessionGeneratorService | Build 78 | Generate deterministic sandbox run documents plus submitted synthetic session documents in `institutes/sim_{simulationId}/academicYears/{yearId}/runs/{runId}/sessions/{sessionId}` using Build 76 environment metadata, Build 77 student profiles, realistic answer/timing behavior, reused submission-metric scoring formulas, and idempotent create-only writes that preserve simulation isolation from production-side triggers
LoadSimulationEngineService | Build 79 | Reuse sandbox simulation sessions plus existing submission, analytics, risk, pattern, and insight services to execute bounded load-scenario samples, measure latency/read-write/conflict metrics, and persist vendor-facing load reports under `vendor/simulationReports/reports/{reportId}` with deterministic low-intensity emulator-safe execution
SimulationValidationService | Build 80 | Validate sandbox intelligence outputs by comparing synthetic student/session expectations against generated `studentYearMetrics` and `runAnalytics` documents, measuring risk-distribution alignment, pattern accuracy, risk-cluster stability, phase-adherence variation, controlled-mode lift, and stability-index behavior in a versioned validation report
VendorIntelligenceService | Build 81 | Initialize the vendor business intelligence foundation with bounded aggregate-source readiness checks, typed pending module states for revenue/layer/churn/adoption/calibration/growth analytics, and structured vendor-only initialization output aligned to the Build 81 platform setup scope
VendorRevenueAnalyticsService | Build 82 | Compute vendor revenue intelligence from aggregated `billingSnapshots` and `vendorAggregates`, normalizing institute billing inputs into current-cycle and time-series revenue outputs including MRR, ARR, revenue by layer, revenue per institute, ARPI/ARPS, month-over-month growth, and revenue volatility
VendorLayerDistributionService | Build 83 | Compute vendor license layer distribution analytics from aggregated `vendorAggregates` and `licenseHistory`, including current layer percentages, adjacent-layer migration velocity, average time spent in each layer, and upgrade frequency by institute size
VendorChurnTrackingService | Build 84 | Compute vendor churn tracking analytics from aggregate billing, institute-summary, license-history, and usage-meter sources, including monthly churn rate, churn by layer and institute size, inactive institute detection, current-cycle downgrade events, and active-student engagement decline
VendorRevenueForecastingService | Build 85 | Compute vendor revenue forecasting analytics from aggregate billing, institute-summary, license-history, and usage-meter sources, including projected MRR and ARR, institute acquisition rate, student volume trends, trailing upgrade probability, and infrastructure cost-to-revenue ratio estimates using the documented scaling cost model
GovernanceSnapshotAggregationService | Build 86, Build 88, Build 101 | Generate immutable governance snapshots in `institutes/{instituteId}/academicYears/{yearId}/governanceSnapshots/{snapshotId}` by aggregating `runAnalytics` and `studentYearMetrics` summary collections, supporting both monthly `YYYY_MM` snapshots and the final archive snapshot keyed by academic year while preserving archive version metadata (`calibrationVersionUsed`, `riskModelVersionUsed`, `templateVersionRangeUsed`) without reading raw session data
GovernanceSnapshotAccessService | Build 89 | Read immutable governance snapshot summaries from `institutes/{instituteId}/academicYears/{yearId}/governanceSnapshots/{YYYY_MM}`, supporting month-specific reads and bounded latest-snapshot retrieval without exposing raw session data
GovernanceReportingService | Build 90 | Generate institute governance reports for `/admin/governance/reports` by composing immutable governance snapshots with month-bounded `overrideLogs` and institute `auditLogs`, emitting monthly governance summaries, major incident alerts, discipline deviation analysis, incident timelines, and PDF export metadata without reading raw session data
ArchivePipelineService | Build 101 | Execute the academic-year archive pipeline for `institutes/{instituteId}/academicYears/{yearId}` by enforcing double confirmation, validating terminal run/session state, locking the academic year, exporting submitted session summaries to `institute_{instituteId}_archive.sessions_{yearId}` in BigQuery, generating the final governance snapshot at `governanceSnapshots/{yearId}`, appending an immutable institute audit log, and marking the academic year as `archived` with idempotent retry behavior
DataRetentionPolicyService | Build 102 | Execute the automated Archive & Data Lifecycle retention pass by loading configurable policy windows from `process.env`, draining Firestore `sessions/{sessionId}` only for archived academic years after a grace window while preserving `runAnalytics` and `studentYearMetrics`, deleting expired root `emailQueue/{emailId}` jobs and `billingRecords/{invoiceId}` documents, and preserving immutable audit and calibration history collections for compliance
StudentDataExportService | Build 103 | Generate secure student compliance exports for `institutes/{instituteId}/students/{studentId}` by collecting the student profile, year-scoped `studentYearMetrics`, session history under `academicYears/{yearId}/runs/{runId}/sessions/{sessionId}`, and optional student `insightSnapshots`, serializing the bundle to CSV, storing it temporarily in secure report storage, generating a signed CDN download URL, and writing an immutable institute `DATA_EXPORT` audit log
StudentSoftDeleteService | Build 104 | Soft-delete institute student identity records by marking `institutes/{instituteId}/students/{studentId}.deleted = true` idempotently, preserving session and analytics datasets, and writing an immutable institute `SOFT_DELETE_STUDENT` audit log for compliance traceability
InterventionToolsService | Build 124 | Power admin intervention workflows by validating action payloads against institute students + `studentYearMetrics`, logging remedial assignment/alert/outcome actions into immutable institute audit logs via `AdministrativeActionLoggingService`, and listing year-scoped intervention timelines from existing audit records without schema expansion
AdminSettingsService | Build 125 | Power admin settings workflows by resolving institute configuration snapshots and applying validated profile, academic-year lock, execution-policy, user-access, security, and feature-flag updates through existing institute-root and academic-year documents, while appending immutable mutation records to `institutes/{instituteId}/settingsAudit/{eventId}` for configuration traceability
FirestoreQueryGovernanceService | Build 56 | Enforce approved Firestore query plans by validating institute-scoped collection paths, indexed filter fields, indexed orderBy fields, bounded limits, and required pagination modes for governed backend query services
StudentQueryCompositeIndexManifest | Build 57 | Define Firestore composite indexes for student admin filtering across `students` and `studentYearMetrics`, covering batch/status/name ordering, status/lastActiveAt ordering, batch/studentId pagination, riskState/disciplineIndex ordering, and avgRawScorePercent/studentId ordering in `firestore.indexes.json`
QuestionBankCompositeIndexManifest | Build 58 | Define Firestore composite indexes for approved `questionBank` filtering across subject/chapter/difficulty, difficulty/lastUsedAt ordering, and status/subject/createdAt ordering in `firestore.indexes.json`
CursorPaginationService | Build 59 | Provide shared cursor-based Firestore pagination utilities enforcing bounded `limit()` + `startAfter()` query windows, limit+1 page reads, and deterministic next-cursor generation for governed list queries
IndexedQueryValidationService | Build 60 | Provide a shared indexed-query validation layer that enforces approved query shapes, institute scoping, and pagination requirements before governed Firestore read services execute
StudentFilteringQueryService | Build 53, Build 104, Build 105 | Execute indexed and paginated institute student filtering by batch on `students/{studentId}` and by `riskState`, `avgRawScorePercent`, and `disciplineIndex` on `academicYears/{yearId}/studentYearMetrics/{studentId}`, with deterministic cursor pagination, bounded student-metrics joins, exclusion of soft-deleted `students/{studentId}` records from operational results, and HOT-only academic-year operational query enforcement
TagDictionaryService | Build 14 | Persist and increment institute-level tag autocomplete metadata in institutes/{instituteId}/tagDictionary/{tagId}
ChapterDictionaryService | Build 15 | Persist and increment institute-level chapter autocomplete metadata in institutes/{instituteId}/chapterDictionary/{chapterId} with normalized chapter and subject
AutocompleteMetadataService | Build 55 | Query institute-level `tagDictionary/{tagId}` and `chapterDictionary/{chapterId}` metadata collections for prefix-based autocomplete suggestions with teacher/admin/internal role enforcement and optional subject filtering for chapters
TemplateCreationService | Build 16 | Validate and normalize newly created institute test templates in institutes/{instituteId}/tests/{testId}, including question ownership, difficulty distribution, and timing profile constraints
TemplateConfigurationSnapshotService | Build 17 | Persist immutable template configuration snapshots (difficultyDistribution, phaseConfigSnapshot, timingProfile) on institutes/{instituteId}/tests/{testId} for assignment-time reuse
AdminTestsService | BWM-013 | Create backend-identified draft templates at version 1; apply expected-version transactional structural updates with immutable versionSnapshots/{version}; and execute idempotent draft-to-ready publish plus ready/assigned-to-archived commands that atomically persist immutable institute audit records
TemplateFingerprintService | Build 18 | Generate and persist deterministic template fingerprints from questionIds, difficultyDistribution, and phaseConfigSnapshot on institutes/{instituteId}/tests/{testId}
TemplateAnalyticsInitializationService | Build 19 | Initialize template analytics stubs in institutes/{instituteId}/academicYears/{yearId}/templateAnalytics/{testId} with deterministic baseline metrics for template effectiveness tracking
TemplateAuditLoggingService | Build 20 | Emit immutable institute template lifecycle audit events (creation, update, activation, archival) via the centralized administrative action logging layer
AssignmentCreationService | Build 21, Build 22, Build 23, Build 100, Build 105, BWM-014-A, BWM-025 | Validate and normalize assignment run creation in institutes/{instituteId}/academicYears/{yearId}/runs/{runId}, including template readiness and expected version, recipient eligibility, license-mode restrictions, scheduled window enforcement, assignment-time immutable template snapshot capture (questionIds, difficultyDistribution, timingProfileSnapshot, and either legacy percentage or Admin-authored rich phase configuration normalized to exact phase percentages), assignment-time version snapshots (`templateVersion`, `riskModelVersion`, `licenseLayer`, `calibrationVersion`), persisted template name for Student/Admin run summaries, HOT-only academic-year enforcement, persisted proctoring policy, and transactional created/replayed idempotency that increments template usage exactly once
AdminRunsService | BWM-014-A/C | Normalize authoritative run creation plus bounded list/detail reads, derive the institute from verified identity at the handler, restrict reads to the resolved current academic year, validate strict persisted run records, apply optional status filtering and deterministic created-at/document-ID cursors, and return not found without probing another tenant or year
StudentSummaryService | BWM-015, BWM-016 | Resolve verified tenant, Student, and license claims to one active non-deleted Student and the current operational academic year; project summary-only dashboard, performance, and L1+ insight data with layer redaction; query only assigned license-eligible runs; provide deterministic status-aware My Tests pagination; and release bounded solution-safe question projections only after current-year completed-run, release-time, and submitted-session ownership checks
RunAnalyticsInitializationService | Build 24, BWM-024 | Initialize deterministic create-only run analytics stubs during assignment creation with baseline metrics plus run/test/batch/mode/status/schedule/participant metadata so exact retries preserve the first authority and Admin summaries are labeled before submissions
UsageMeteringService | Build 25, Build 91, Build 104 | Track institute billing usage metrics in `institutes/{instituteId}/usageMeter/{cycleId}`, including assignment counts, assigned student volume, active-student lifecycle totals, submitted session volume, peak usage, pricing-plan-derived billing projections, and idempotent assignment/student/session event deduplication, while excluding soft-deleted students from active billing counts
BillingSnapshotService | Build 92 | Generate immutable root-level `billingSnapshots/{instituteId}__{cycleId}` records from institute `usageMeter/{cycleId}` summaries plus current license metadata, including cycle boundaries, license tier, peak usage, projected invoice amounts, and dispute-protection webhook status fields for downstream vendor analytics
SimulationStudentGeneratorService | Build 77 | Generate deterministic synthetic students in `institutes/sim_{simulationId}/students` using Build 76 environment metadata, behavioral-profile attributes (`baselineAbility`, `disciplineProfile`, `impulsivenessScore`, `overconfidenceScore`, `fatigueFactor`, `topicStrengthMap`), and idempotent create-only writes
DataTierPartitionService | Build 105 | Resolve HOT/WARM/COLD academic-year and run partition state from existing lifecycle documents, map operational collections to documented storage tiers, and block non-HOT academic years from active execution and operational query flows
SystemEventTopologyService | Build 106, Build 107, Build 110 | Centralize the unified deterministic event topology registry across content, template, assignment, session-execution, post-submission, vendor-intelligence, and archive-lifecycle domains; validate single-primary-handler and acyclic downstream relationships at startup; enforce trigger-handler alignment through typed event-dispatch guards; orchestrate the architecture-defined master lifecycle sequence (question upload → template creation → assignment creation → session execution → submission analytics → insights → governance scheduling → vendor aggregation → billing meter update → archive) with runtime emission guards across API, trigger, and scheduled boundaries; and enforce final topology safety validation for event-order correctness, self-referential trigger-loop prevention, and an acyclic engine dependency graph tied to lifecycle events
PostSubmissionPipelineService | Build 109, BWM-024 | Centralize deterministic submitted-session post-processing orchestration (analytics trigger markers, usage metering, run/student/question analytics, insights, and notifications), then mark the newest result propagation available only after every step completes so direct execution and recovery replay share one idempotent path
FailureRecoveryService | Build 109 | Persist and process idempotent post-submission retry jobs under `systemFlags/failureRecoveryRetryQueue/jobs/{jobId}`, dispatch retries via Cloud Tasks `failureRecoveryDispatch`, escalate persistent failures to `systemFlags/failureRecoveryDeadLetterQueue/jobs/{jobId}`, and provide scheduled fallback queue draining without impacting submission confirmation flows

---

# PROCESSING ENGINES

Engine | Build | Purpose
---|---|---
RunAnalyticsEngine | Build 41 | Compute incremental run-level analytics from submitted session events and persist aggregates in `runAnalytics/{runId}`
QuestionAnalyticsEngine | Build 42 | Compute incremental question-level aggregates from submitted session events and persist them in `questionAnalytics/{questionId}`
StudentMetricsEngine | Build 43, Build 87 | Compute incremental yearly student aggregates from submitted session events and persist them in `studentYearMetrics/{studentId}`, including governance-ready stability trend inputs for downstream snapshot computation
RiskEngine | Build 44 | Compute deterministic student-level risk scores, rolling risk windows, and cluster classifications from `studentYearMetrics/{studentId}` updates
PatternEngine | Build 45 | Behavioral pattern detection
InsightEngine | Build 46 | Generate deterministic student, run, and batch insight snapshots from analytics outputs without scanning raw session collections

---

# FIRESTORE COLLECTIONS

Collection | Scope | Description
---|---|---
institutes | Global | Institute root namespace
auditLogs | Global | Immutable system-wide audit records
vendorAuditLogs | Global | Immutable vendor governance audit records
auditLogs | Institute | Immutable institute-scoped audit records, including deterministic question bulk and managed-asset mutation audits
students | Institute | Student records, including Build 104 soft-delete state via `deleted = true` for compliance-driven deletion requests that preserve sessions and analytics
questionBank | Institute | Authoritative versioned question records with canonical relative managed question/solution asset paths
questionUploadLogs | Institute | Deterministic immutable question-bulk commit records keyed by normalized payload fingerprint for exact retry replay
tagDictionary | Institute | Autocomplete metadata for normalized question tags
chapterDictionary | Institute | Autocomplete metadata for normalized chapters per subject
questionAnalytics | Institute | Question-level analytics stubs and aggregates
tests | Institute | Test templates
runs | AcademicYear | Test assignments
sessions | Run | Exam execution sessions
runAnalytics | AcademicYear | Run-level analytics, metadata/completion authority, newest-result propagation state, and deterministic per-session processing-marker subcollection
studentYearMetrics | AcademicYear | Student yearly performance metrics, newest-result propagation state, deterministic per-session processing markers, and Student-owned summary-only `results/{runId}` records
governanceSnapshots | AcademicYear | Immutable monthly governance snapshot records
insightSnapshots | AcademicYear | Generated student, run, and batch insight snapshots
emailQueue | Global | Root-level asynchronous notification queue jobs created by post-submission notification processing
billingSnapshots | Global | Immutable billing-cycle dispute-protection snapshots and vendor billing analytics inputs
globalCalibration | Global | Canonical immutable calibration model version documents for vendor-managed behavioral weighting and threshold configuration
calibrationVersions | Global | Compatibility mirror of immutable calibration model version documents for the Build 96 calibration flow entry path
vendorCalibrationLogs | Global | Immutable vendor-scoped calibration deployment history records for activation auditability and rollback tracing
usage | Institute | Billing usage metering
usageMeter | Institute | Billing and assignment usage metering summaries by cycle
license | Institute | License configuration
licenseHistory | Institute | Immutable institute license change records
calibrationHistory | Institute | Immutable institute-scoped mirrors of vendor calibration deployment history
overrideLogs | Institute | Immutable institute execution override records
settingsAudit | Institute | Immutable institute settings mutation audit records produced by Build 125 admin settings APIs

---

# FRONTEND MODULES

Module | Portal | Description
---|---|---
AdminQuestionLibraryBoundary | Shared/Admin | BWM-012 added the shared `AdminQuestionLibraryResult` DTO and strict `adaptAdminQuestionLibraryResult` boundary used by all six live ADM-06 Admin consumers; it validates the full row shape, requires canonical path/signed-preview pairs, rejects direct Storage endpoints or missing signature parameters, and supplies signed question/solution previews to question detail rendering
AdminQuestionPackageUploadWorkflow | Admin | BWM-012 retains referenced PNG/WebP bytes from a validated ZIP, binds them to ADM-09-authoritative IDs/versions, uploads question and solution assets sequentially through ADM-18 before commit, preserves retry access after handled failures, and commits only the returned canonical managed paths
AdminDashboard | Admin | Institute overview dashboard
StudentDashboard | Student | Student performance summary
ExamRunner | Exam | Exam session interface
VendorDashboard | Vendor | Vendor analytics interface
MultiPortalRoutingFramework | Shared | Build 66 shared portal routing foundation covering canonical domain mapping, route-family resolution, secure redirects, role/layer guard evaluation, and lazy-loaded admin/student/exam/vendor portal shells
AdminPortalRoutes | Admin | Build 67 admin portal route registry and access-control layer covering overview, students, tests, assignments, analytics, insights, governance, settings, licensing, dynamic path resolution, and role/license-gated navigation
StudentPortalRoutes | Student | Build 68 student portal route registry and access-control layer covering dashboard, my-tests, performance, insights, discipline, profile, dynamic performance detail resolution, and license-gated navigation
ExamPortalExecutionRoute | Exam | Build 69 exam portal execution route registry and bootstrap shell covering `/session/{sessionId}`, signed token enforcement, student-only access, redirect-on-invalid-entry behavior, immutable snapshot loading sequence, and exam runtime handoff preparation
VendorPortalRoutes | Vendor | Build 70 vendor portal route registry and access-control layer covering overview, institutes, institute detail, licensing, calibration, calibration simulation/history, intelligence, revenue, system health, and audit navigation on `vendor.yourdomain.com/vendor/*`
MultiPortalAppScaffold | Shared | Build 111 initialized independent frontend apps under `apps/admin`, `apps/student`, `apps/exam`, and `apps/vendor` so each portal can run/build independently with aligned Vite+TypeScript scaffolding
PortalManifestService | Shared | Build 111 shared `shared/services/portalManifest.ts` domain-map module defining canonical admin/student/exam/vendor portal metadata for cross-portal reuse
PortalTitleHook | Shared | Build 111 shared `shared/hooks/usePortalTitle.ts` helper to apply consistent portal-specific document titles across frontend apps
PortalShellUiFoundation | Shared | Build 111 shared UI foundation under `shared/ui` (`portal-shell.css`, `portalShellModel.ts`) for reusable shell styling and metadata rendering support across portals
FrontendEnvironmentConfig | Shared | Build 112 shared typed frontend environment contract via `shared/types/frontendEnvironment.ts` and `shared/services/frontendEnvironment.ts`, plus per-portal `.env.example` templates for Vite Firebase keys
FrontendFirebaseClientService | Shared | Build 112 shared `shared/services/firebaseClient.ts` lazy Firebase app/auth bootstrap for portal runtime initialization
FrontendStackRuntimeBootstrap | Shared | Build 112 wired React Router + Firebase bootstrap into `apps/admin|student|exam|vendor/src/main.tsx`, configured student/exam/vendor base routes, and standardized formatting tooling (`.prettierrc.json`, `.prettierignore`, per-portal format scripts)
SharedUiComponentLibrary | Shared | Build 113 added reusable frontend UI primitives in `shared/ui/components` (navigation bar, table, form fields, modal dialog, chart container, pagination) with shared styling in `shared/ui/components/shared-ui-components.css`
PortalSharedUiConsumption | Shared | Build 113 integrated shared UI primitives across `apps/admin/src/App.tsx` and `apps/student|exam|vendor/src/App.tsx` so all portals consume the shared component system for deterministic UI consistency
FrontendApiClientLayer | Shared | Build 114 added centralized typed API client contracts in `shared/types/apiClient.ts` and shared API client runtime in `shared/services/apiClient.ts`, including Firebase ID token attachment, retry policy handling, and standardized `ApiClientError` responses with optional `VITE_API_BASE_URL` support
FrontendAuthProviderService | Shared | Build 115 added shared authentication contracts in `shared/types/authProvider.ts` and provider runtime in `shared/services/authProvider.tsx`, including Firebase auth-state subscription, sign-in/sign-out methods, session persistence, and periodic ID-token refresh support
FrontendAuthRouteGuards | Shared | Build 115, BWM-008 integrated protected-route and login-route enforcement across portal apps (`apps/student|exam|vendor/src/App.tsx`) with deterministic unauthenticated redirects to `/student/login`, `/session/login`, and `/vendor/login`; Student and Vendor authenticated admission additionally derives its allowed role from the canonical `portal.student.access` and `portal.vendor.access` capability entries and redirects wrong-role sessions to `/unauthorized` while server middleware remains authoritative
FrontendAuthRuntimeBootstrap | Shared | Build 115 wrapped all portal entry points (`apps/admin|student|exam|vendor/src/main.tsx`) with the shared `AuthProvider` so authentication context is accessible across portal runtimes
AdminPortalLayoutShell | Admin | Build 116 implemented the admin portal core layout in `apps/admin/src/App.tsx` with sidebar navigation, top header bar, main content container, and route-based rendering for `/admin/overview`, `/admin/students`, `/admin/tests`, `/admin/assignments`, `/admin/analytics`, and `/admin/settings`
AdminStudentManagementInterface | Admin | Build 117 implemented the `/admin/students` management workspace in `apps/admin/src/features/students/StudentManagementPage.tsx` with shared `UiTable` + `UiForm` components, search/filter controls, batch assignment actions, activation/deactivation toggles, edit-details modal workflow, and mobile-safe responsive behavior wired into `apps/admin/src/App.tsx`
AdminTestTemplateManagementInterface | Admin | Build 118 / BWM-013 implemented the `/admin/tests` management workspace in `apps/admin/src/features/tests/TestTemplateManagementPage.tsx`; BWM-013 made live create draft-only through `POST /admin/tests`, structural edit optimistic and versioned through `PATCH /admin/tests/{testId}`, and publish/archive distinct expected-version commands through `POST /admin/tests/{testId}/publish|archive`. Every mutation consumes its strict shared result, reloads ADM-10, and reconciles backend identity, structural version, and lifecycle status before replacing UI state; the frontend never invents a template ID.
AdminAssignmentManagementInterface | Admin | Build 119 / BWM-014 implemented the `/admin/assignments` management workspace in `apps/admin/src/features/assignments/AssignmentManagementPage.tsx` with shared `UiForm` + `UiTable` components, template and canonical execution-mode selection, student-batch targeting with recipient de-duplication, authoritative idempotent assignment scheduling via `POST /admin/runs`, and exact persisted replay reconciliation through `assignmentAuthority.ts`. Live list state comes only from strict current-year `GET /admin/runs` records with server status filtering and opaque-cursor pagination; `AdminAssignmentDetailPage.tsx` consumes strict `GET /admin/runs/{runId}` lifecycle, recipient, schedule, and policy authority and explicitly does not substitute Admin Analytics or fixtures. The Auth/Firestore/Functions/Hosting Chromium proof covers create, replay, fresh-document reload, detail, authorization, recipients, and concurrent convergence.
AdminAnalyticsDashboard | Admin | Build 120 implemented the `/admin/analytics` dashboard in `apps/admin/src/features/analytics/AdminAnalyticsDashboardPage.tsx` with shared `UiChartContainer` visualizations (score distribution, accuracy metrics, risk clusters, discipline index statistics), run performance summary table via shared `UiTable`, summary-only data handling from `runAnalytics` + `studentYearMetrics`, deterministic local fixtures, and responsive route integration in `apps/admin/src/App.tsx`
AdminSummaryLiveContracts | Admin | BWM-011 aligned `GET /admin/overview` and `GET /admin/analytics` with the standard API success envelope and strict summary-only response adapters, added exact handler-to-adapter and seeded emulator/browser contract coverage, and made failed live reads render explicit shared data states while fixture content remains hidden. BWM-024 makes both routes consume propagated run/Student summaries, collapse lifecycle and analytics records by run ID, and prefer the analytics projection without fixture or raw-session reads.
AdminRiskInsightsDashboard | Admin | Build 121 implemented the `/admin/analytics/risk-insights` dashboard in `apps/admin/src/features/analytics/RiskInsightsDashboardPage.tsx` with shared `UiChartContainer` pie distribution for risk clusters, shared `UiTable` high-risk student listings, guess-rate indicators, discipline trend cards, reusable analytics hydration contracts in `apps/admin/src/features/analytics/analyticsDataset.ts`, and route integration in `apps/admin/src/App.tsx`
AdminBatchAnalyticsDashboard | Admin | Build 122 implemented the `/admin/analytics/batch` dashboard in `apps/admin/src/features/analytics/BatchAnalyticsDashboardPage.tsx` with batch performance comparison tables (`UiTable`), time-series average score trend visualizations using the new `line` variant in `shared/ui/components/UiChartContainer.tsx`, batch discipline and risk distribution charts from summary-only `runAnalytics` + `studentYearMetrics` data, analytics cross-link routing updates in `apps/admin/src/App.tsx`, route-registry coverage in `apps/admin/src/portals/adminRoutes.ts`, and deterministic browser verification artifacts under `apps/admin/artifacts/build-122/`
AdminGovernanceMonitoringDashboard | Admin | Build 123 implemented the `/admin/governance` monitoring dashboard in `apps/admin/src/features/analytics/GovernanceMonitoringDashboardPage.tsx` with typed governance snapshot hydration in `apps/admin/src/features/analytics/governanceDataset.ts` via `POST /admin/governance/snapshots`, institutional stability/phase-adherence/override/execution-integrity KPI cards, month-to-month governance comparisons, trend visualizations plus latest risk-cluster distribution using shared `UiChartContainer`, snapshot timeline table via shared `UiTable`, route and sidebar integration in `apps/admin/src/App.tsx`, analytics cross-link update in `apps/admin/src/features/analytics/AdminAnalyticsDashboardPage.tsx`, responsive styling in `apps/admin/src/App.css`, and deterministic browser verification artifacts under `apps/admin/artifacts/build-123/`
AdminInterventionTools | Admin | Build 124 implemented `/admin/insights/interventions` in `apps/admin/src/features/insights/InterventionToolsPage.tsx` with high-risk student targeting from summary-only `studentYearMetrics` inputs, remedial assignment + alert + outcome action controls backed by `POST /admin/interventions`, intervention audit timeline rendering from immutable institute audit records, cross-link integration from `RiskInsightsDashboardPage`, route/sidebar integration in `apps/admin/src/App.tsx`, responsive interaction styling in `apps/admin/src/App.css`, and deterministic browser verification artifacts under `apps/admin/artifacts/build-124/`
AdminSettingsConfigurationWorkspace | Admin | Build 125 implemented the admin settings and system-configuration experience in `apps/admin/src/features/settings/AdminSettingsConfigurationPage.tsx` with sectioned workflows for institute profile, academic year controls, execution policy, user/role access, security settings, and feature flags; typed data adapter `apps/admin/src/features/settings/settingsDataset.ts` wired to `POST /admin/settings` and `POST /admin/academicYear/archive`; route integration for `/admin/settings`, `/admin/settings/profile`, `/admin/settings/academic-year`, `/admin/settings/execution-policy`, `/admin/settings/users`, `/admin/settings/security`, and `/admin/settings/system` in `apps/admin/src/App.tsx`; responsive styling in `apps/admin/src/App.css`; and deterministic browser verification artifacts under `apps/admin/artifacts/build-125/`
StudentPortalLayoutShell | Student | Build 126 implemented the student portal core layout in `apps/student/src/App.tsx` with an authenticated shell that protects `/student/dashboard`, `/student/my-tests`, `/student/performance`, `/student/insights`, and `/student/profile`; top navigation bar + sidebar menu + route-rendered main content container; login fallback route at `/student/login`; responsive styling in `apps/student/src/App.css`; Vite React dedupe alignment in `apps/student/vite.config.ts`; and deterministic browser verification artifacts under `apps/student/artifacts/build-126/`
StudentDashboardUi | Student | Build 127 implemented the `/student/dashboard` interface in `apps/student/src/features/dashboard/StudentDashboardPage.tsx`; BWM-015 aligns `studentDashboardDataset.ts` with the strict shared `StudentDashboardResult`, honors backend current-year authority, and hydrates live mode only from identity-scoped `GET /student/dashboard`. BWM-024 adds Student-owned propagated recent results while keeping upcoming tests and metrics summary-only, assigned/licensed, layer-redacted, and free of fixture/raw-session reads.
StudentMyTestsInterface | Student | Build 128 implemented the `/student/my-tests` interface in `apps/student/src/features/my-tests/StudentMyTestsPage.tsx`; BWM-015 aligns `studentMyTestsDataset.ts` with the strict shared `StudentTestsResult` and hydrates live scheduled/active/completed/archived views only from bounded identity-scoped `GET /student/tests` calls. BWM-017 and BWM-018 prove the real launch/entry boundary. BWM-024 fills completed score/risk/time/question fields from `studentYearMetrics/{studentId}/results/{runId}` and proves the refreshed completed view without fixture or raw-session reads.
StudentPerformanceAnalyticsInterface | Student | Build 129 implemented the `/student/performance` interface in `apps/student/src/features/performance/StudentPerformancePage.tsx` with typed performance normalization and license-layer redaction. BWM-024 sources its bounded chronological result timeline from Student-owned `results/{runId}` summaries, preserving the yearly aggregate for headline and topic metrics without fixture fallback or session scans.
StudentInsightsInterface | Student | Build 130 implemented the `/student/insights` interface in `apps/student/src/features/insights/StudentInsightsPage.tsx` with constructive behavior and topic-weakness interpretation, summary-only access guardrails, and L1 gating. BWM-024 proves that submission-pipeline `insightSnapshots` plus propagated `studentYearMetrics` summaries refresh the live page alongside the other Student result projections.
ExamInterfaceLayoutShell | Exam | Build 131 implemented the instruction-first `/session/:sessionId` exam entry and layout shell in `apps/exam/src/App.tsx` with token-gated access validation, mandatory declaration-gated instruction screen, mode-specific instruction injection (Operational/Diagnostic/Controlled/Hard), global countdown timer header, left vertical status palette with section filters, base question rendering container handoff for Build 132, and deterministic browser verification artifacts under `apps/exam/artifacts/build-131/`
ExamQuestionRenderingEngine | Exam | Build 132 implemented the question rendering engine in `apps/exam/src/App.tsx` + `apps/exam/src/App.css` with session-snapshot-backed text/image/media question presentation, typed MCQ/numeric/matrix response handling, question-level controls (Previous, Clear Response, Mark for Review, Save & Next), status-driven palette transitions with section filters and direct jump behavior, Hard Mode-compatible revisit locks, and header-launched client-only scientific calculator modal (no backend persistence/history), verified via deterministic browser artifacts under `apps/exam/artifacts/build-132/`
ExamNavigationTimingInterface | Exam | Build 133 implemented the navigation and timing interface in `apps/exam/src/App.tsx` + `apps/exam/src/App.css` with header subject tabs, candidate identity and question-count metrics, server-authoritative countdown sync cadence, final-window timer emphasis, auto-submit-on-expiry flow, layer-based execution behavior (`L0` free mode, `L1` advisory diagnostics, `L2` MinTime gating + slowdown enforcement, Hard Mode MinTime/MaxTime locking + restricted submit), lightweight adaptive phase telemetry (phase adherence, overspend, difficulty compliance, skip pattern), and deterministic browser verification artifacts under `apps/exam/artifacts/build-133/`
ExamAnswerSubmissionInteraction | Exam | Build 134 and BWM-022 implement typed response queueing in `apps/exam/src/ExamRuntimeApp.tsx`; monotonic client revisions/timestamps; serialized batch identity/sequence and exact acknowledgement matching; revision-safe queue removal; max-ten scheduled/heartbeat writes; complete reconnect and pre-submit multi-batch drains; owner/session-bound IndexedDB recovery with preserved counters; explicit Offline/Syncing/Error pending telemetry; and Firebase SDK-managed authenticated runtime calls
ExamSubmissionWorkflow | Exam | Build 135, BWM-020, and BWM-023 implement deterministic submission orchestration in `apps/exam/src/ExamRuntimeApp.tsx` with server-authoritative lifecycle tracking (`created`/`started`/`active`/`submitted`/`expired`/`terminated`), secured EXM-05 activation after declaration, server-clock/deadline countdown reconciliation, complete manual pre-submit drain, server-aware expiry finalization, an exact four-field EXM-04 request, strict consumption and display of server-owned status/reason/time/replay disposition and raw/accuracy/discipline/guess/phase/min/max/risk metrics, one-active-session-per-run guard, owner-bound IndexedDB recovery, and immutable post-submit interaction lock
VendorPortalLayoutShell | Vendor | Build 136 implemented the vendor portal layout shell in `apps/vendor/src/App.tsx` with vendor-only authentication + role guard enforcement, sidebar/top-header/main-content route architecture, and route-based rendering for `/vendor/overview`, `/vendor/institutes`, `/vendor/licensing`, `/vendor/calibration`, `/vendor/intelligence`, `/vendor/system-health`, and `/vendor/audit`; added typed executive snapshot data contracts in `apps/vendor/src/features/overview/vendorOverviewDataset.ts` and overview rendering in `apps/vendor/src/features/overview/VendorOverviewPage.tsx` for required platform metrics (`TotalInstitutes`, `ActiveInstitutes`, `TotalActiveStudents`, `TotalMonthlyTestRuns`, `GlobalRiskDistribution`, `GlobalDisciplineIndex`, `MonthlyRecurringRevenue`, `InfrastructureCostEstimate`, `SystemErrorRate`) sourced from `vendorMetrics` summary semantics; and added deterministic browser verification artifacts under `apps/vendor/artifacts/build-136/`
VendorInstituteManagementInterface | Vendor | Build 137 implemented institute management in `apps/vendor/src/features/institutes/VendorInstituteManagementPage.tsx` with typed institute-sourced dataset contracts in `apps/vendor/src/features/institutes/vendorInstitutesDataset.ts`, institute listing table, search/filter controls, institute profile + activity metrics view, license-layer overview cards, and vendor-authoritative lifecycle actions (`ViewInstitute`, `SuspendInstitute`, `UpgradeLicense`, `DowngradeLicense`, `ExtendLicense`, `ForceArchive`, hard-guarded `DeleteInstitute`) wired into `/vendor/institutes` via `apps/vendor/src/App.tsx`
VendorLicensingSubscriptionControl | Vendor | Build 137 implemented licensing and subscription controls in `apps/vendor/src/features/licensing/VendorLicensingPage.tsx` with institute-scoped subscription status, billing cycle, next invoice, payment failure visibility, manual override workflow, webhook log viewer, and license change history sourced from typed dataset helpers; integrated `/vendor/licensing` route in `apps/vendor/src/App.tsx`; and added deterministic browser verification artifacts under `apps/vendor/artifacts/build-137/`
VendorCalibrationManagementInterface | Vendor | Build 138 implemented calibration management in `apps/vendor/src/features/calibration/VendorCalibrationManagementPage.tsx` with typed calibration contracts in `apps/vendor/src/features/calibration/vendorCalibrationDataset.ts`, parameter editor guardrails (risk-weight sum enforcement, range validation, timing multiplier bounds), summary-only simulation workflow (`studentYearMetrics`, `runAnalytics`, `riskComponents`, `disciplineComponents`) integrated with `POST /vendor/calibration/simulate` and deterministic local fallback, impact comparison views (risk distribution shift, discipline delta, cluster movement, batch-level delta, stability impact), push workflows via `POST /vendor/calibration/push` supporting global or selected institute scope, schedule + draft mode controls, and rollback queue support without historical recomputation
VendorAuditActivityLogsInterface | Vendor | Build 138 implemented immutable vendor calibration activity visibility in `apps/vendor/src/features/audit/VendorAuditActivityLogsPage.tsx`, sourcing append-only records through shared calibration dataset helpers, filterable action/scope search, actor-attributed timestamped event rendering for calibration pushes, manual overrides, simulations, and rollback events on `/vendor/audit`; integrated `/vendor/calibration` and `/vendor/audit` routes in `apps/vendor/src/App.tsx`; added responsive styling updates in `apps/vendor/src/App.css`; and added deterministic browser verification artifacts under `apps/vendor/artifacts/build-138/`
VendorIntelligenceDashboardInterface | Vendor | Build 139 implemented `/vendor/intelligence` in `apps/vendor/src/features/intelligence/VendorIntelligenceDashboardPage.tsx` with macro-level cross-institute and business KPI rendering (MRR, institute growth rate, license layer distribution, churn indicators, average revenue per institute, upgrade conversion rate, active student growth), trend visualizations and behavior-signal charts via shared `UiChartContainer`, discipline/business/weakness cluster tables via shared `UiTable`, and strict summary-only boundary messaging for aggregate sources
VendorIntelligenceDatasetAdapter | Vendor | Build 139 implemented typed intelligence contracts in `apps/vendor/src/features/intelligence/vendorIntelligenceDataset.ts` using aggregate-only source semantics (`vendorAggregates`, `billingRecords`, `licenseHistory`) with monthly revenue + growth trend points, discipline index by exam type, global hard-bias/easy-neglect/controlled-effectiveness signal records, and topic weakness cluster summaries; integrated route wiring in `apps/vendor/src/App.tsx`, responsive chart support updates in `apps/vendor/src/App.css`, and deterministic browser verification artifacts under `apps/vendor/artifacts/build-139/`
VendorSystemHealthMonitoringDashboard | Vendor | Build 140 implemented `/vendor/system-health` in `apps/vendor/src/features/system-health/VendorSystemHealthDashboardPage.tsx` with system health KPI cards (Firestore read/write counts, function invocations, cost/error/failure indicators), infrastructure alert highlighting, global feature-flag control workflows (`EnableBetaFeatures`, `EnableExperimentalRiskEngine`, `EnableNewUI`, `SetRolloutPercentage`) mapped to `globalFeatureFlags/{flagName}`, snapshot-only data export/backup operations (`ExportPlatformMetrics`, `ExportInstituteData`, `TriggerManualBackup`, `RestoreSimulationEnvironment`), and structural guarantee checklist visibility aligned with vendor architecture Sections 1.5.8, 1.5.9, 1.5.11, and 1.5.13
VendorSystemHealthDatasetAdapter | Vendor | Build 140 implemented typed system-health contracts in `apps/vendor/src/features/system-health/vendorSystemHealthDataset.ts` using architecture-aligned source semantics (`platform metrics`, `error logs`, `monitoring dashboards`) with trend points, alert severity records, middleware-enforced feature flag document-path metadata, snapshot-only operation descriptors, and structural guarantee evidence entries; integrated route wiring in `apps/vendor/src/App.tsx`, responsive route styling updates in `apps/vendor/src/App.css`, and deterministic browser verification artifacts under `apps/vendor/artifacts/build-140/`
FrontendRouteChunkingStrategy | Shared | Build 141 implemented portal route-level lazy loading and code splitting across admin/student/vendor app shells in `apps/admin/src/App.tsx`, `apps/student/src/App.tsx`, and `apps/vendor/src/App.tsx` using `React.lazy` + `Suspense` boundaries, preserving existing routing/guard behavior while reducing initial feature bundle cost
UiRouteLoadingFallback | Shared | Build 141 implemented reusable lazy-route fallback UI in `shared/ui/components/UiRouteLoading.tsx` with shared export wiring in `shared/ui/components/index.ts` and styling in `shared/ui/components/shared-ui-components.css` for deterministic cross-portal route-loading states
ExamRuntimeDeferredChunk | Exam | Build 141 split the exam runtime into a deferred module by moving runtime-heavy implementation to `apps/exam/src/ExamRuntimeApp.tsx` and using a lazy wrapper entry in `apps/exam/src/App.tsx`, keeping secure route behavior (`/`, `/session/:sessionId`, wildcard guard) unchanged while reducing initial exam entry parse load; BWM-019 removes production `buildSessionSnapshot` use, hydrates that chunk from strict EXM-01 server authority, and isolates the optional mock snapshot behind a directly eliminable development-only import guard
FrontendBuild141VerificationArtifacts | Shared | Build 141 added deterministic cross-portal browser verification harness `artifacts/build-141/verify-routes.mjs` with route/viewport console+network+responsive+guard evidence persisted in `artifacts/build-141/verification-results.json` and captured screenshots for affected routes
CdnAssetDeliveryService | Shared | Build 142 added shared frontend CDN asset resolution and path-builder utilities in `shared/services/cdnAssetDelivery.ts`, including `VITE_CDN_BASE_URL`-aware URL normalization, direct-storage URL translation safeguards, question asset URL builders, and student report URL builders for deterministic portal asset delivery
StudentCdnAssetAdapter | Student | Build 142 updated `apps/student/src/features/my-tests/studentMyTestsDataset.ts` to normalize solution images and summary report links through CDN URL resolution for both fallback fixtures and API payload hydration, preserving lazy-loaded current-year solution behavior and archived summary-only restrictions
ExamCdnQuestionAssetLoader | Exam | Build 142 updated `apps/exam/src/ExamRuntimeApp.tsx` to load question images via CDN asset URLs, preserve next-question-only preload behavior via normalized CDN URLs, and avoid direct storage endpoint exposure in exam question rendering
FrontendBuild142VerificationArtifacts | Shared | Build 142 added deterministic student/exam browser verification harness `artifacts/build-142/verify-routes.mjs` with desktop/mobile route checks and console/network/responsive/guard evidence persisted in `artifacts/build-142/verification-results.json` plus captured screenshots
FrontendMonitoringTelemetryService | Shared | Build 143 added shared frontend monitoring utilities in `shared/services/frontendMonitoring.ts` + `shared/types/frontendMonitoring.ts` to capture runtime exceptions, unhandled promise rejections, API failure metadata, API timing metrics, navigation timing metrics, session identifiers, actor attribution, and append-only audit-style telemetry records for critical events
FrontendApiMonitoringInstrumentation | Shared | Build 143 updated `shared/services/apiClient.ts` to emit deterministic API timing and failure telemetry (method/path/url/status/attempt/duration/code/message) on final request outcomes without changing existing retry behavior
PortalTelemetryBootstrap | Frontend | Build 143 initialized frontend telemetry bootstrap in `apps/admin/src/main.tsx`, `apps/student/src/main.tsx`, `apps/vendor/src/main.tsx`, and `apps/exam/src/main.tsx` so each portal registers global runtime error and rejection handlers with portal attribution
VendorFrontendTelemetryDashboardExtension | Vendor | Build 143 extended `apps/vendor/src/features/system-health/vendorSystemHealthDataset.ts` and `apps/vendor/src/features/system-health/VendorSystemHealthDashboardPage.tsx` to surface frontend telemetry summary KPIs (runtime exceptions, failed API calls, average API latency) and critical event tables with session/actor/time metadata
FrontendBuild143VerificationArtifacts | Shared | Build 143 added deterministic cross-portal browser verification harness `artifacts/build-143/verify-routes.mjs` with desktop/mobile route checks and console/network/responsive/guard evidence persisted in `artifacts/build-143/verification-results.json` plus captured screenshots
FrontendPortalCiCdWorkflow | Shared | Build 144 added GitHub Actions frontend CI/CD automation in `.github/workflows/frontend-ci-cd.yml` with branch-based environment promotion (`dev`, `staging`, `main`), portal lint/type-build/test validation gates, Firebase target application, and deterministic hosting deployment sequencing
PortalHostingBundlePreparationService | Shared | Build 144 added `scripts/frontend-cicd/prepare-portal-hosting.mjs` to assemble admin and student build artifacts into `.firebase/hosting/portal` with deterministic `/admin` and `/student` path isolation and portal-root redirect bootstrap
PortalPathBasedBuildBaseConfig | Shared | Build 144 updated `apps/admin/vite.config.ts` and `apps/student/vite.config.ts` to support `VITE_BASE_PATH`-driven base URL normalization for path-scoped deployment assets (`/admin/` and `/student/`) without impacting local default builds
FirebaseFrontendMultiSiteHostingConfig | Shared | Build 144 updated `firebase.json` hosting topology from single-site to multi-target (`portal`, `exam`, `vendor`) with architecture-aligned SPA rewrites and portal route isolation for admin/student/exam/vendor delivery domains
FrontendBuild144VerificationArtifacts | Shared | Build 144 added deterministic cross-portal browser verification harness `artifacts/build-144/verify-routes.mjs` with desktop/mobile route checks and console/network/responsive/guard evidence persisted in `artifacts/build-144/verification-results.json` plus captured screenshots
CrossPortalAuthSessionBridge | Shared | Build 145 bridge retired by BWM-009: `shared/services/crossPortalAuthSession.ts` was removed and no application runtime persists or restores reusable ID tokens through local storage or JavaScript-readable cross-domain cookies
FrontendAuthCrossPortalBridgeIntegration | Shared | Build 145 integration retired by BWM-009: `shared/services/authProvider.tsx` keeps tokens in memory, `shared/services/apiClient.ts` fails closed without a current-origin Firebase user, and `shared/services/legacyBrowserTokenStorage.ts` performs delete-only cleanup of the two legacy storage keys and cookie
BrowserBearerTokenStorageHardening | Shared | BWM-009 added permanent source/cleanup contracts plus an Auth/Firestore/Functions/Hosting Chromium proof that Admin and Student purge legacy copies, do not persist loopback fallback tokens, do not restore them after a new document load, and send no retired bearer token to the gateway
PerOriginFirebaseAuthentication | Shared | BWM-009 selected Firebase Auth SDK-managed per-origin persistence: Admin/Student share their intentional Portal origin, Exam and Vendor retain independent origin sessions, no cross-origin token exchange is added, and a runtime/build/CI-gated loopback-only Auth emulator setting supports real browser sign-in regressions without entering release artifacts
ProductionLoginFallbackHardening | Shared | BWM-009 removed every prefilled Admin/Student/Vendor login credential, made synthetic credential authentication a compile-time development-only plus exact-loopback capability, retained explicit development-mode browser coverage, and added production-loopback denial plus clean four-portal release-artifact proofs
FrontendBuild145VerificationArtifacts | Shared | Build 145 added deterministic browser verification harness `artifacts/build-145/verify-routes.mjs` with desktop/mobile checks for cross-portal auth continuity, vendor RBAC blocking, signed exam-token route access, and console/network/responsive/guard evidence in `artifacts/build-145/verification-results.json`
PortalIntegrationLayerService | Shared | Build 146 added `shared/services/portalIntegration.ts` to centralize portal login/default route conventions, environment-aware portal URL resolution (`portal`, `exam`, `vendor` base URLs), and shared per-portal API client provisioning
PortalApiClientStandardization | Shared | Build 146 migrated admin/student/vendor feature API usage to `getPortalApiClient(...)` and moved Exam answer/submit calls to the shared API client instead of direct `fetch`; BWM-018 added authenticated entry and retired the former `/token/refresh` caller in favor of Firebase SDK refresh
FrontendBuild146VerificationArtifacts | Shared | Build 146 added deterministic browser verification harness `artifacts/build-146/verify-routes.mjs` with desktop/mobile checks for admin login conventions, student cross-portal continuity, vendor RBAC/login behavior, exam token guard/session entry, and console/network/responsive/guard evidence in `artifacts/build-146/verification-results.json`
GlobalPortalStateContracts | Shared | Build 147 added typed global-state contracts in `shared/types/globalPortalState.ts` for license object modeling (layer/limits/eligibility/feature flags/status), backend-enforcement capability mapping, global rollout flags, portal permissions, and environment/session state snapshots
GlobalPortalStateService | Shared | Build 147 added `shared/services/globalPortalState.tsx` with centralized ID-token claim decoding + normalization, shared license/feature-flag/global-rollout derivation, backend-matrix-aligned capability checks (`ControlledMode`, `GovernanceDashboard`, `HardMode`, `AdaptivePhase`), and `GlobalPortalStateProvider` + `useGlobalPortalState` access across portal modules
PortalGlobalStateBootstrap | Frontend | Build 147 wrapped all portal runtime entries (`apps/admin|student|exam|vendor/src/main.tsx`) with `GlobalPortalStateProvider` so authenticated session, role/permissions, feature flags, and environment configuration state are accessible from shared frontend context
PortalAccessStateUnification | Shared | Build 147 migrated admin/vendor access context resolution to the shared global-state resolver in `apps/admin/src/portals/adminAccess.ts` and `apps/vendor/src/portals/vendorAccess.ts`, and migrated student license-aware routing/page checks in `apps/student/src/App.tsx` plus `apps/student/src/features/dashboard|performance|insights` to consume centralized license-layer state instead of per-file JWT parsing
FrontendBuild147VerificationArtifacts | Shared | Build 147 added deterministic browser verification harness `artifacts/build-147/verify-routes.mjs` with desktop/mobile checks for admin login conventions, student route/license guard behavior, vendor RBAC/login behavior, exam token guard/session entry, and console/network/responsive/guard evidence in `artifacts/build-147/verification-results.json`
FrontendClientCrashTelemetry | Shared | Build 148 extended `shared/types/frontendMonitoring.ts` + `shared/services/frontendMonitoring.ts` with typed `captureFrontendClientCrash(...)` emission for centralized `client_crash` monitoring events carrying timestamp/session/actor/request metadata and append-only critical-event audit mapping
UiGlobalErrorBoundary | Shared | Build 148 added shared render-failure containment UI in `shared/ui/components/UiErrorBoundary.tsx` with user-safe fallback messaging, retry/reload controls, event/timestamp trace metadata, and local crash-probe support for deterministic verification of fallback behavior
PortalErrorBoundaryBootstrap | Frontend | Build 148 wrapped all portal runtime entries (`apps/admin|student|exam|vendor/src/main.tsx`) with shared `UiErrorBoundary` so render-time failures are consistently contained without full-app crashes across all portals
FrontendBuild148VerificationArtifacts | Shared | Build 148 added deterministic browser verification harness `artifacts/build-148/verify-routes.mjs` with desktop/mobile checks for crash-fallback UI, login/guard continuity, and console/network/responsive/guard evidence in `artifacts/build-148/verification-results.json`
PortalUiConsistencyNavigationModel | Shared | Build 149 added `shared/ui/portalConsistency.ts` as the centralized navigation contract for Admin/Student/Vendor primary route ordering, labels, summaries, and active-route resolution behavior aligned with architecture sections `1.2.1.1` and `1.5.1`
PortalUiThemeTokenLayer | Shared | Build 149 added `shared/ui/portal-theme.css` with shared portal-level typography/focus/surface tokens to enforce cross-portal baseline consistency while preserving portal-specific styles (including exam visual constraints)
PortalNavigationConsistencyIntegration | Frontend | Build 149 updated `apps/admin/src/App.tsx`, `apps/student/src/App.tsx`, and `apps/vendor/src/App.tsx` to consume shared navigation definitions instead of duplicated local arrays, standardizing navigation behavior and active-route matching logic
FrontendBuild149VerificationArtifacts | Shared | Build 149 added deterministic browser verification harness `artifacts/build-149/verify-routes.mjs` with desktop/mobile checks for admin/student navigation continuity, student license guard behavior, vendor role guard behavior, exam token guards, and console/network/responsive/guard evidence in `artifacts/build-149/verification-results.json`
FrontendBuild150PlatformValidationArtifacts | Shared | Build 150 added deterministic final platform validation harness `artifacts/build-150/verify-routes.mjs` and evidence set `artifacts/build-150/verification-results.json` with desktop/mobile checks for cross-portal authentication flows, admin governance/settings/licensing structural-guarantee routes, student performance and L1 guard behavior, exam token-guard plus instruction/runtime continuity, vendor non-vendor RBAC guard behavior, and vendor institutes/licensing/intelligence/system-health operational routes including console/network/responsive/guard status capture and screenshots per route

---

# REGISTRY UPDATE RULE

After completing each build:

1. Identify newly created modules
2. Add them to this registry
3. Record the build number

This ensures the platform remains deterministic.
